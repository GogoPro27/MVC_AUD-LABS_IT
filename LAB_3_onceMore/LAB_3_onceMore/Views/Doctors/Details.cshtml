@model LAB_3_onceMore.Models.HospitalModels.Doctor

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Doctor</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

    </dl>
</div>
<div>
    @Model.Name `s Patients:
    <ul>
        @foreach (var pat in Model.Patients)
        {
            <li>
                @Html.ActionLink(pat.Name, "Patients", "Details", new { id = pat.Id })
            </li>
        }
    </ul>

</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |
    @Html.ActionLink("Add patient", "AddPatient", new { id = Model.Id }) |
    @Html.ActionLink("Back to List", "Index")
</p>
